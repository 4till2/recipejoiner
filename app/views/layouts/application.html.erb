<!DOCTYPE html>
<html>
<head>
  <title>Spoonjoy</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body>
<main class="w-full mt-24 md:mt-28 px-4 flex">
  <%= render 'shared/navbar' %>
  <% if notice.present? %>
    <p class="absolute top-8 left-8 py-2 px-3 bg-green-50 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
  <% end %>
  <%= yield %>
</main>
</body>
</html>
